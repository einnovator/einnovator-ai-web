{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PRA website benchmark export",
  "description": "Missing values must be explicit strings such as Not measured; numeric zero must never stand in for missing evidence.",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["id", "stage", "metric", "value", "comparison", "quality", "model", "engine", "engineVersion", "hardware", "workload", "status", "evidence", "provenance", "date", "methodology", "source"],
    "additionalProperties": false
  }
}
