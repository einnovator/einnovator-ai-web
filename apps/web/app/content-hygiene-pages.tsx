import Link from "next/link";
import publications from "@/data/publications.json";
import { praDocsUrl } from "@/lib/content";
import { Arrow, Breadcrumbs, CodeBlock, CtaBand, PageHero, SectionTitle, Status } from "./components";

export function PublicationsReworkedPage() {
  return <><Breadcrumbs items={[{ label: "Research", href: "/research" }, { label: "Publications" }]} /><PageHero eyebrow="PRA publications" title="Mechanisms, evidence, and limitations in the open." intro="The commercial site uses product language; these public research links provide the deeper mechanisms, experimental context, corrections, and negative results behind it." /><section className="section wrap"><div className="publication-list">{publications.map((paper) => <a href={paper.href} key={paper.title}><span>{paper.type}</span><div><h2>{paper.title}</h2><p>{paper.description}</p></div><Arrow /></a>)}</div></section><CtaBand title="Turn research evidence into a deployment decision." /></>;
}

export function AgentReworkedPage() {
  return <><Breadcrumbs items={[{ label: "PRA", href: "/products/pra" }, { label: "Agent" }]} /><PageHero eyebrow="PRA Agent · Available reference application" title="Treat tasks, tools, results, sessions, and source records as first-class context." intro="PRA Agent is an optional open-source reference surface for durable agent work. The same runtime also fits existing agents through the Gateway, SDK, and typed event bridges, so adopting PRA does not require replacing the application loop." actions={<><Link className="button" href="/developers/quickstart">Run PRA Agent <Arrow /></Link><a className="button button-ghost" href={praDocsUrl("agents/index.html")}>Agent integration guide</a></>} /><section className="engine-summary"><div className="wrap"><Status>Available</Status><span>CLI, TUI, durable sessions, tasks, tools, and typed records</span></div></section><section className="section wrap"><SectionTitle eyebrow="Context model" title="Stop replaying the whole working set on every turn." /><div className="feature-cards"><article><span>01</span><h3>Sessions retain stable identity</h3></article><article><span>02</span><h3>Tasks define execution scope</h3></article><article><span>03</span><h3>Tools and results stay addressable</h3></article><article><span>04</span><h3>Exact source records remain recoverable</h3></article></div><div className="agent-start"><div><h3>Interactive work</h3><CodeBlock>{`pra agent chat Qwen/Qwen3-0.6B -w . -t "Inspect this repository"`}</CodeBlock></div><div><h3>Noninteractive run</h3><CodeBlock>{`pra agent run -p work "Summarize the current task state."`}</CodeBlock></div></div></section><section className="section section-tint"><div className="wrap"><SectionTitle eyebrow="Integration choices" title="Preserve the semantics the client can actually carry." copy="An OpenAI-compatible base URL is enough for chat transport. Typed PRA records require the extension envelope or a family-specific bridge." /><div className="deployment-grid"><article><Status>Available</Status><h3>PRA Agent</h3><p>The complete product path for persistent sessions, typed records, task state, lazy tools and skills, and explicit transport negotiation.</p></article><article><Status>Available</Status><h3>Gateway / direct runtime</h3><p>Ordinary chat remains compatible; capability inspection determines whether typed records, deltas, or native behavior survive the boundary.</p></article><article><Status>Bridge path</Status><h3>DeepSeek Harness and Pi</h3><p>Typed tool-result bridges preserve stable, deduplicated resource identity while the external agent keeps its tool loop.</p></article><article><Status>Documented boundary</Status><h3>OpenHands, Aider, Codex CLI, Claude Code</h3><p>Each integration guide distinguishes basic model connectivity from typed PRA semantics instead of implying unsupported transport.</p></article></div></div></section><section className="section wrap"><div className="two-column"><SectionTitle eyebrow="Local operator surface" title="The Agent Web UI is experimental." copy="It uses the same profiles, sessions, records, tasks, tools, and runtime as the CLI; it is not a second agent implementation." /><div><ul className="check-list"><li>Local browser conversations, chat, task state, and record inspection</li><li>Typed tool-approval events scoped to one request</li><li>Loopback binding by default</li><li>No production authentication layer</li></ul><p className="honesty-note">Keep the Web UI on loopback unless an authenticated reverse proxy, TLS, network controls, rate limits, request limits, and audit policy are in place.</p><a className="inline-link" href={praDocsUrl("web-ui.html")}>Review Web UI operations <Arrow /></a></div></div></section><CtaBand title="Bring PRA to the agent you already run." /></>;
}

const solutions = {
  "long-context": { eyebrow: "Long-context systems", title: "Keep the working set large without replaying it all.", intro: "Turn documents, history, tools, and task state into addressable resources and materialize only the selected detail.", points: ["Bound visible context", "Preserve exact source detail", "Measure quality against Full Context", "Choose text or native execution by evidence"] },
  "agent-infrastructure": { eyebrow: "Agent infrastructure", title: "Give agents durable context without an ever-growing prompt.", intro: "Represent sessions, tasks, tools, results, documents, and history as typed, scoped records that can be selected and recovered.", points: ["Typed records", "Task execution scope", "Authorization-aware selection", "Existing-agent compatibility"] },
  "inference-optimization": { eyebrow: "Inference optimization", title: "Optimize context as a first-class serving resource.", intro: "Compare Selected Context with Native Memory at matched quality, then qualify the path that improves latency, throughput, memory, transfer, or reuse.", points: ["Selected Context baseline", "Engine-specific native qualification", "Explicit residency lifecycle", "Quality-adjusted metrics"] },
  "enterprise-ai": { eyebrow: "Enterprise AI", title: "Make context efficiency measurable, reversible, and supportable.", intro: "Start with existing applications and inference endpoints, preserve tenant and authorization boundaries, and qualify each deeper optimization against a frozen baseline.", points: ["Ordinary-text fallback", "No retraining requirement", "Tenant and session isolation", "Reproducible assessment"] },
} as const;

export function SolutionReworkedPage({ slug }: { slug: string }) {
  const copy = solutions[slug as keyof typeof solutions] ?? solutions["long-context"];
  return <><PageHero eyebrow={copy.eyebrow} title={copy.title} intro={copy.intro} actions={<><Link className="button" href="/developers/quickstart">Try PRA <Arrow /></Link><Link className="button button-ghost" href="/products/pra/benchmarks">View evidence</Link></>} /><section className="section wrap"><div className="feature-cards">{copy.points.map((point, index) => <article key={point}><span>{String(index + 1).padStart(2, "0")}</span><h3>{point}</h3></article>)}</div></section><section className="section section-tint"><div className="wrap two-column"><SectionTitle eyebrow="Adoption" title="Start compatible. Promote only with evidence." /><div><p>Use Selected Context through an existing endpoint to establish the workload baseline. Native Memory and Native Serving are measured optimizations, not entry requirements.</p><Link className="inline-link" href="/products/pra/integrations">Choose an engine recommendation <Arrow /></Link></div></div></section><CtaBand /></>;
}
