import Link from "next/link";
import { Arrow, Breadcrumbs, CtaBand, PageHero, SectionTitle, Status } from "./components";

const evaluationSteps = [
  ["01", "Baseline Full Context", "Pin the model, engine, hardware, workload, traffic shape, and current prompt strategy."],
  ["02", "Freeze quality and evidence", "Agree on task success, safety constraints, datasets, statistical treatment, and fallback thresholds."],
  ["03", "Measure Selected Context", "Route and select ordinary context, then measure tokens, quality, TTFT, throughput, and memory."],
  ["04", "Qualify Native Memory", "Only when reuse is relevant, freeze selection and measure its incremental encoding, transfer, memory, and serving effect."],
  ["05", "Qualify Native Serving", "Only when the engine owns the seam, measure prefetch, residency, transfer, tails, and useful throughput."],
  ["06", "Recommend mode and profile", "Choose the shallowest path that improves the measured quality-cost frontier with a clear fallback."],
  ["07", "Deliver a reproducible report", "Provide configuration, methods, results, limitations, open gates, and a production integration plan."],
];

export function EnterpriseReworkedPage() {
  return <><Breadcrumbs items={[{ label: "PRA", href: "/products/pra" }, { label: "Enterprise" }]} /><PageHero eyebrow="PRA for enterprise" title="Adopt context optimization without betting the platform." intro="Begin with Selected Context in front of the model server you already operate. Preserve an ordinary-text fallback, measure quality first, and add native depth only where a named workload proves its value." actions={<><Link className="button" href="/company/contact">Request an assessment <Arrow /></Link><Link className="button button-ghost" href="/products/pra/benchmarks">Review evidence discipline</Link></>} /><section className="section wrap"><SectionTitle eyebrow="Deployment risk" title="The control plane stays explicit." copy="PRA separates semantic relevance, authorization, session identity, and physical model memory so a retrieval hit never becomes an implicit access decision." /><div className="risk-grid"><article><h3>Ordinary-text fallback</h3><p>Every native path can downgrade to selected ordinary context through the established engine boundary.</p></article><article><h3>No retraining requirement</h3><p>Selected Context changes request assembly, not model weights.</p></article><article><h3>No raw K/V on the agent wire</h3><p>Applications exchange logical resource identities and content; native memory stays inside the qualified inference boundary.</p></article><article><h3>Capability negotiation</h3><p>Gateway and engine paths advertise what they support instead of silently assuming native behavior.</p></article><article><h3>Tenant and session identity</h3><p>Resource, session, tenant, model, profile, and engine identity travel as distinct lifecycle dimensions.</p></article><article><h3>Authorization before materialization</h3><p>Retrieval relevance does not grant access. Authorization remains an independent gate.</p></article></div></section><section className="section section-tint"><div className="wrap"><SectionTitle eyebrow="Optimization Assessment" title="A benchmark engagement that ends in a decision." copy="The assessment is a paid or partner-scoped evaluation—not an open-ended experiment and not a preselected native recommendation." /><div className="engagement-steps">{evaluationSteps.map(([number, title, copy]) => <article key={number}><span>{number}</span><div><h3>{title}</h3><p>{copy}</p></div></article>)}</div></div></section><section className="section wrap"><SectionTitle eyebrow="Commercial offers" title="What eInnovator provides beyond open source." /><div className="offer-grid"><article><Status>Evaluate</Status><h3>Optimization Assessment</h3><p>Matched baseline, quality gate, PRA comparison, and reproducible recommendation.</p></article><article><Status>Build</Status><h3>Production Integration</h3><p>Gateway, SDK, records, profiles, observability, engine integration, and rollout support.</p></article><article><Status>Operate</Status><h3>Enterprise Support</h3><p>Priority debugging, compatibility guidance, private validation, upgrades, and deployment review.</p></article><article><Status>Partner</Status><h3>Engine Partnership / Native Integration</h3><p>Capability seams, cache coexistence, isolation, lifecycle ownership, and joint qualification.</p></article></div><p className="honesty-note"><strong>PRA is under active systems qualification.</strong> Commercial support accelerates integration and evidence; it does not turn an unmeasured configuration into a performance guarantee.</p></section><CtaBand title="Turn your workload into a reproducible decision." /></>;
}

export function GatewayReworkedPage() {
  return <><Breadcrumbs items={[{ label: "PRA", href: "/products/pra" }, { label: "Gateway" }]} /><PageHero eyebrow="PRA Gateway · Compatibility boundary" title="Put Selected Context in front of an existing model endpoint." intro="The Gateway is the low-friction application path: preserve an OpenAI-compatible boundary, select authorized ordinary context, negotiate deeper capability explicitly, and retain a reversible text fallback." /><section className="section wrap"><div className="compatibility-flow" aria-label="PRA Gateway deployment"><div><span>Existing application</span><small>messages · tools · session</small></div><i>→</i><div className="accent"><span>PRA Gateway</span><small>identity · authorization · routing · selection</small></div><i>→</i><div><span>Existing model server</span><small>selected ordinary context</small></div></div></section><section className="section section-tint"><div className="wrap"><SectionTitle eyebrow="Contract" title="Logical resources cross the application boundary. Native memory does not." /><div className="risk-grid"><article><h3>Selected Context first</h3><p>Send ordinary messages or text to any compatible endpoint without an engine patch.</p></article><article><h3>Explicit capabilities</h3><p>Native features require advertised support, matched identity, and a validated downgrade path.</p></article><article><h3>Session deltas</h3><p>Carry stable session and resource changes without replaying the entire logical working set.</p></article><article><h3>Prefix coexistence</h3><p>Keep exact prefix-cache identity separate from query-addressed semantic-resource identity.</p></article><article><h3>Tenant isolation</h3><p>Do not share or attach native state across tenant, session, model, profile, or authorization boundaries.</p></article><article><h3>Authorization independent of relevance</h3><p>A highly relevant record is still excluded when the caller is not authorized to materialize it.</p></article></div><p className="honesty-note"><strong>Current status:</strong> the public product direction and compatibility contract are defined; a production Gateway command is not represented here as generally available until the reviewed CLI exposes it.</p></div></section><CtaBand title="Evaluate the Gateway against your current endpoint." /></>;
}

export function DesignPartnersReworkedPage() {
  return <><PageHero eyebrow="PRA design partners" title="Benchmark PRA against a workload that matters." intro="Design partnerships use the same disciplined assessment path as enterprise evaluation: a frozen baseline, explicit quality gates, reversible deployment, and a report that retains negative results." actions={<Link className="button" href="/company/contact">Apply with a workload <Arrow /></Link>} /><section className="section wrap"><div className="partner-grid"><article><span>Partners provide</span><ul className="check-list"><li>A representative workload and current baseline</li><li>A quality and safety evaluation harness</li><li>Target engine, model, hardware, and traffic shape</li><li>Named engineering and product contacts</li></ul></article><article><span>eInnovator provides</span><ul className="check-list"><li>Selected Context integration and matched measurement</li><li>Optional Native Memory and Native Serving qualification</li><li>Profile calibration and fallback design</li><li>A reproducible report and production recommendation</li></ul></article></div></section><section className="section section-tint"><div className="wrap"><SectionTitle eyebrow="Engagement path" title="Baseline first. Native only if the workload calls for it." /><div className="engagement-steps compact">{evaluationSteps.slice(0, 6).map(([number, title, copy]) => <article key={number}><span>{number}</span><div><h3>{title}</h3><p>{copy}</p></div></article>)}</div><p className="honesty-note">The program is selective and scoped. No customer logo, case study, metric, or production claim is published without review and explicit permission.</p></div></section><CtaBand title="Bring the workload you want to improve." /></>;
}
