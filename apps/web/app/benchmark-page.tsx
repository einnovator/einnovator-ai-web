import { benchmarks } from "@/lib/content";
import { BenchmarkExplorer } from "./benchmark-explorer";
import { Breadcrumbs, CtaBand, PageHero, SectionTitle, Status } from "./components";

export function BenchmarksReworkedPage() {
  return <><Breadcrumbs items={[{ label: "PRA", href: "/products/pra" }, { label: "Evidence" }]} /><PageHero eyebrow="PRA evidence and qualification" title="Compare each optimization against the right baseline." intro="Selected Context is the compatibility baseline. Native Memory or Native Serving is recommended only when it improves the measured quality-cost frontier." /><section className="section wrap"><div className="gain-model"><article id="context-optimization"><span>Context gain</span><h2>Full Context → Selected Context</h2><p>Input-token reduction, quality delta, TTFT, throughput, and peak memory where measured.</p></article><article id="native-memory-reuse"><span>Native gain</span><h2>Selected Context → Native Memory</h2><p>Encoding cost, TTFT, throughput, native K/V bytes, transfer, reuse, and memory.</p></article><article id="serving-optimization"><span>Serving gain</span><h2>Native Memory → Native Serving</h2><p>Prefetch, shared residency, transfer, tail latency, and useful throughput.</p></article></div></section><section className="section section-tint"><div className="wrap"><div className="method-callout"><div><span className="kicker">Method principle</span><h2>Freeze selection and quality before comparing infrastructure.</h2></div><p>Each row must name the model, engine version, hardware, workload, evidence level, provenance, date, and methodology. Native depth earns promotion only when its incremental contribution survives the matched comparison.</p></div><BenchmarkExplorer rows={benchmarks} /></div></section><section className="section wrap"><SectionTitle eyebrow="Evidence labels" title="A status is part of every claim." /><div className="evidence-levels"><article><Status>Measured</Status><h3>Result exists</h3><p>The displayed claim has a named source and bounded configuration.</p></article><article><Status>Qualification in progress</Status><h3>Gate remains open</h3><p>A mechanism or partial result exists, but the recommendation is not complete.</p></article><article><Status>Blocked</Status><h3>Evidence failed a gate</h3><p>A negative result is retained because it changes the current recommendation.</p></article><article><Status>Not applicable</Status><h3>No claim</h3><p>The mode or metric does not apply, or the required system is not defined.</p></article></div><p className="honesty-note"><strong>PRA is under active systems qualification.</strong> Every published metric is tied to a model, engine, hardware environment, workload, and evidence level. Recommendations are updated when new measurements change the frontier.</p></section><CtaBand title="Bring your own evaluation harness." copy="eInnovator can baseline Full Context, freeze quality evidence, measure Selected Context, and qualify native paths only where they improve the frontier." /></>;
}
